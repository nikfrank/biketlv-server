{
  "name": "bike_tlv",
  "version": "1.0.0",
  "description": "server for bike_tlv",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run pre-deploy && npm run mid-deploy && npm run finish-deploy",
    "pre-deploy": "cp -rL build dist && git branch -D deploy && git checkout -b deploy",
    "mid-deploy": "sed -i /dist/d ./.gitignore && git add . && git commit -am deploy",
    "finish-deploy": "git push origin deploy --force && git checkout master"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nikfrank/biketlv-server.git"
  },
  "keywords": [
    "server",
    "bike",
    "tlv"
  ],
  "author": "nachum_freedman",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/nikfrank/biketlv-server/issues"
  },
  "homepage": "https://github.com/nikfrank/biketlv-server#readme",
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "morgan": "^1.9.1",
    "pg": "^7.5.0",
    "sequelize": "^4.39.0"
  }
}
